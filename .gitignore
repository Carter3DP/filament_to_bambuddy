# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Local learning cache (barcode → filament details)
barcode_cache.json

# Local reusable spool barcode -> Bambuddy spool ID map
spool_barcodes.json
printer_barcodes.json

# Open Filament Database index (downloaded + rebuilt daily)
ofd_index.json

# SpoolmanDB-Community index (downloaded + rebuilt daily)
spoolmandb_community_index.json

# Local run script / env with secrets — never commit
run.sh
.env
*.local

# OS / editor
.DS_Store
.idea/
.vscode/
